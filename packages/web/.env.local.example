# Terminal server ports (5080 range chosen to avoid conflicts with dev tools)
TERMINAL_PORT=5080                  # ttyd (iframe) terminal server
DIRECT_TERMINAL_PORT=5081           # Direct WebSocket terminal server with XDA support

# Public environment variables (exposed to browser)
NEXT_PUBLIC_TERMINAL_PORT=5080
NEXT_PUBLIC_DIRECT_TERMINAL_PORT=5081
